# GitLab Handbook Chatbot

An AI-powered chatbot that allows users to query GitLab's Handbook and Direction pages using natural language. Built with a RAG (Retrieval-Augmented Generation) pipeline, Groq LLM, and Streamlit.

## 🔗 Live Demo
[Click here to try the chatbot](https://app-chatbot-bafhtdynpfqgf8q5ufxmhm.streamlit.app)

---

## How It Works

1. **Scraping** — GitLab Handbook pages are scraped using `BeautifulSoup`
2. **Chunking & Embedding** — Text is split into 500-word chunks and converted to vector embeddings using `sentence-transformers`
3. **Storage** — Embeddings are stored locally in a `data.json` file
4. **Retrieval** — On each query, cosine similarity (via `numpy`) finds the top 3 most relevant chunks
5. **Generation** — Groq's Llama 3.1 model generates an answer using the retrieved context
6. **UI** — Streamlit provides a clean, interactive chat interface

---

## Project Structure

```
gitlab-chatbot/
├── scraper.py        # Scrapes GitLab Handbook pages
├── ingest.py         # Chunks text, creates embeddings, saves to data.json
├── chatbot.py        # RAG pipeline — retrieval + Groq LLM
├── app.py            # Streamlit chat UI
├── data.json         # Pre-computed embeddings (generated by ingest.py)
├── requirements.txt  # Python dependencies
└── .env              # API keys (not committed to GitHub)
```

---

## Local Setup Instructions

### 1. Clone the Repository
```bash
git clone https://github.com/Machgahe/gitlab-chatbot.git
cd gitlab-chatbot
```

### 2. Create a Virtual Environment
```bash
python -m venv venv
```

Activate it:
- **Windows:** `venv\Scripts\activate`
- **Mac/Linux:** `source venv/bin/activate`

### 3. Install Dependencies
```bash
pip install -r requirements.txt
```

### 4. Set Up API Key
Create a `.env` file in the root folder:
```
GROQ_API_KEY=your_groq_api_key_here
```

Get a free Groq API key at [console.groq.com](https://console.groq.com)

### 5. (Optional) Re-scrape and Re-ingest Data
The `data.json` file is already included. Only run this if you want to refresh the data:
```bash
python ingest.py
```

### 6. Run the App
```bash
streamlit run app.py
```

The app will open automatically at `http://localhost:8501`

---

## Sample Questions to Try

- What are GitLab's core values?
- How does GitLab communicate asynchronously?
- What is GitLab's hiring process?
- How does GitLab approach diversity and inclusion?
- What is GitLab's remote work policy?

---

## Tech Stack

| Component | Technology |
|---|---|
| Language | Python 3.x |
| LLM | Groq API (Llama 3.1 8B) |
| Embeddings | sentence-transformers (all-MiniLM-L6-v2) |
| Vector Search | NumPy cosine similarity |
| Web Scraping | BeautifulSoup + Requests |
| UI | Streamlit |
| Deployment | Streamlit Community Cloud |

---

## Dependencies

```
streamlit
groq
sentence-transformers
beautifulsoup4
requests
python-dotenv
numpy
```